{{<layout}}
{{$content}}
<br><br>
<a href="/settings/vacation/new" class="btn btn-small btn-success">Create new Vacation</a>
<br><br>
<div class="panel panel-default">

    <div class="panel-heading">Vacations</div>

    <!-- Table -->
    <table class="table">
        <thead>
            <tr>
                <th>Type</th>
                <th>Balance</th>
                <th>Description</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            {{#vacations}}
            <tr>
                <td>{{type}}</td>
                <td>{{balance}}</td>
                <td>{{description}}</td>
                <td><a href="delete/id/{{id}}" class="delete_row  btn btn-small btn-primary"> Deactivate</a>
                    &nbsp;<a href="edit/id/{{id}}" class="btn btn-small btn-warning" >Edit</a>
            </tr>
            {{/vacations}}
        </td>
        </tr>
        </tbody>
    </table>
</div>

{{/content}}
{{/layout}}
